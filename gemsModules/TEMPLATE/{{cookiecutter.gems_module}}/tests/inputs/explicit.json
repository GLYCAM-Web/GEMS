{
    "entity": {
        "type": "{{ cookiecutter.gems_module }}",
        "inputs": {
            "{{ cookiecutter.basic_explicit_key }}": "{{ cookiecutter.basic_explicit_value }}"
        }
    }
}